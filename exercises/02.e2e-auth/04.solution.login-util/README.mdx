# Login Page utility

Great! Now starting off a new test with a logged in user is as simple as
calling `login()` 🎉. We've avoided wasteful and error prone "repeat testing" by
leveraging utility functions. Great job.
