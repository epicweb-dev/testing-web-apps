# Fixtures

👨‍💼 Great, when you run the test: `npm run test:e2e` you'll now have a file
created with the contents of any email sent. We already have our `.gitignore`
setup to ignore our directory of email fixtures (it's no good to commit
generated files). The onboarding process should now be fully tested! We have a
lot more confidence that things are working.

But, there's one last thing we need to consider before we say this is finished...
