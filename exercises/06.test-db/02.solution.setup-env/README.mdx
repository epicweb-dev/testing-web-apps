# Reuse Setup with setupFiles

🧝‍♂️ Great job. It's feeling cleaner already. Now that this is applied to all
tests, we should probably consider more than just the `cities` table when doing
the cleanup... Let's tackle that next.
